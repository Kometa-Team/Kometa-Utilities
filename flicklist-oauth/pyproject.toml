[project]
name = "flicklist-oauth"
version = "1.0.0"
description = "Flask app serving the FlickList OAuth device-code auth page"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0.0",
    "gunicorn>=21.2.0",
]

[dependency-groups]
dev = [
    "pytest>=7.4.0",
    "pytest-flask>=1.3.0",
]
